# 요구사항 엔지니어링 시스템 — 설계서 (DESIGN)

> **AI 협업 기반 요구사항 엔지니어링 시스템**
> 고객 요구사항을 검토·확정하고, 필요한 산출물을 도출하며, 요구사항↔산출물의 연결과 변경을 추적한다.
> SEMES · VCS 파트 과제 · 문서 최종 정리본 (기획 확정 기준)

관련 문서: [기능명세서](docs/기능명세서.md) · [API 명세서](docs/API명세서.md) · [화면 정의(기능1)](docs/화면정의_기능1.html)

---

## 1. 개요

### 1.1 배경
SEMES는 삼성전자로부터 요구사항 명세서를 받아 **VCS**(Vehicle Control System — Probe Card를 옮기는 AMR을 제어)를 개발한다. 이 과정에서 반복되는 세 가지 문제를 앞단에서 해결하는 것이 이 시스템의 목표다.

| # | 문제 | 설명 |
|---|---|---|
| 1 | **고객 요구사항의 불명확한 표현** | 명세서에 고객사·세메스가 다르게 해석하거나 추가 정의가 필요한 문장이 많다 |
| 2 | **문서 작업량 과다** | 요구사항마다 기능·비기능 명세와 Jira 이슈를 개발자가 일일이 작성 → 공수가 크다 |
| 3 | **변경 추적 어려움** | 요구사항이 바뀔 때마다 영향받는 기능·문서를 일일이 찾아 확인·수정해야 한다 |

> 이 도구는 "수작업을 없앤다"가 아니라 **틀(템플릿) + 초안**으로 방대한 문서 작성을 크게 덜어 주고, 착수 전에 불명확·상충을 잡아 재작업을 줄인다.

### 1.2 핵심 기능 3가지

| 기능 | 이름 | 한 줄 설명 | 상태 |
|---|---|---|---|
| **1** | 요구사항 검토 | 불명확·상충되는 요구를 LLM이 검출·제안 → 협의해 확정 → 버전으로 관리 | 개발 중 |
| **2** | 산출물 도출 | 확정 요구사항에서 산출물 5종 초안을 자동 도출 | 개발 예정 |
| **3** | 산출물 관리 (추적성) | 요구사항↔산출물 연결·변경 영향·진행상황 관리 | 개발 예정 |

흐름: **요구사항 검토 → 산출물 도출 → 추적성 관리**

### 1.3 운영 환경 · 제약
- **폐쇄망**: 반입(inbound)만 허용, **반출(outbound) 차단**. 외부 AI API(Claude/GPT 등) 사용 불가.
- **AI는 사내에서만**: 사내 LLM API(GPT-OSS-120B, OpenAI 호환 `POST /v1/chat/completions`)로만 추론. 응답이 없으면 규칙 기반으로 동작한다. 모델 가중치는 저장소에 포함하지 않고, 주소·모델명은 소스가 아니라 환경 변수로 넘긴다(`ai-model/README.md`).
- **사내 프록시**가 SSL 검사를 하여 npm/pip/Maven 등이 막히므로, 배포 시 사전 빌드 산출물(프론트 정적 빌드, 백엔드 jar)을 함께 제공한다.

---

## 2. 시스템 아키텍처

폐쇄망 안의 3계층 + 데이터베이스.

```
[웹 UI · React(Next.js)]  →  [백엔드 · Spring Boot(Java 21)]  →  [AI 서버 · FastAPI]  →  사내 GPT-OSS-120B / 규칙 기반
                                        │
                                   [Oracle DB]
```

| 계층 | 역할 | 구현 |
|---|---|---|
| 프론트엔드 | 로그인·대시보드·검토·산출물·추적성 화면 | React (Next.js) |
| 백엔드 | 요구사항·버전·산출물·추적성·사용자 관리, AI 중개 | Spring Boot (Java 21) |
| AI 서버 | 무상태 검출·생성 추론 | FastAPI (Python) |
| 모델 | 검출·제안·도출 추론 | 사내 GPT-OSS-120B(OpenAI 호환 API), 미응답 시 규칙 기반 폴백 |
| DB | 요구사항·버전·검출·산출물·링크·사용자 저장 | Oracle |

- AI 서버는 **무상태**: 요청마다 프롬프트를 받아 결과(JSON)만 반환. 상태·이력은 백엔드/DB가 관리.
- 모델 미기동 시 **규칙 기반 mock**으로 폴백해 데모·개발이 끊기지 않게 한다.

---

## 3. 핵심 기능 1 — 요구사항 검토

고객 요구사항을 그대로 문서로 옮기기 전에, **불명확한 표현**과 **서로 어긋나는 요구(상충)**를 먼저 찾아 정리한다. 동작 흐름은 **LLM이 검출·제안 → 담당자가 고객사와 협의 → 확정하면 버전이 부여**다. AI는 문제를 찾아 방향을 제안할 뿐, 확정은 사람이 내린다.

### 3.1 불명확한 표현 검출 (7종 유형)
수치·기준·주체·조건이 빠져 해석이 갈리는 표현을 찾아 유형·근거와 함께 제시한다.

| 유형 | 설명 | 예시 |
|---|---|---|
| 정량 기준 부재 | 수치·기준값 없이 정성적으로만 표현 | "충분한 내구성을 가져야 한다" |
| 모호한 정도부사 | 사람마다 기준이 다른 부사 | "적절히 조정한다" |
| 주어/주체 불명확 | 누가 수행하는지 미명시 | "관련 부서와 협의하여 처리한다" |
| 조건 발생 시점 불명확 | 트리거 조건 미정의 | "필요한 경우 개선한다" |
| 예외/경계 조건 누락 | 정상만 정의, 예외 없음 | "확인된 것으로 간주한다" |
| 접속사 범위 모호 | and/or·"등"의 범위가 불명확 | "… 등 상세 조건" |
| 시간·일정 모호 | 구체적 기한 없음 | "빠른 시일 내 대응한다" |

- 근거가 없으면 지어내지 않고 "판정 불가"로 둔다.
- "정의되지 않음"으로 단정하기보다 **"해석이 갈릴 수 있음 / 기준이 필요함"** 톤으로 제시(고객 문서 존중).

### 3.2 상충되는 요구 검출
여러 요구사항을 함께 보고 서로 모순되는지를 찾는다. 불명확이 "한 문장 안"의 문제라면, 상충은 "요구사항들 사이"의 문제다.
- 출력: 충돌하는 요구사항 쌍(**REQ-ID로 상호 참조**) + 무엇이 어떻게 어긋나는지 + 해소 제안.
- REQ-ID 참조는 나중에 어느 요구사항을 고쳐야 하는지 추적성(기능 3)과 연결된다.

### 3.3 AI 검토 & 수정 (검토 화면)
검출된 항목마다 다음을 한 화면에서 처리한다.
- **AI 제안**: 각 검출 항목에 대해 "이렇게 쓰면 명확" 초안을 제시. [본문에 반영]으로 원클릭 편집.
- **수정**: 본문(내용·비고)을 직접 편집. 변경 부분을 하이라이트.
- **재검토**: 수정 후 다시 AI 검토를 돌릴 수 있다.
- **넘어가기(해결 불필요)**: 문맥상 문제없다고 판단하면 넘긴다(기록에는 남음).

### 3.4 확정 & 버전 부여
해석을 확정하면 **시맨틱 버전**이 부여된다(3.6). 확정 시 **변경 사유(커밋 메시지)**를 입력하며, 이 문구가 버전 이력의 제목이 된다. AI가 diff를 보고 변경 사유 초안을 제안할 수 있다.
- 첫 확정: 검토중 → 확정, **v1.0.0** 부여.
- 확정본을 다시 수정·확정: 변경 유형에 따라 다음 버전(예: v1.0.0 → v1.0.1).

### 3.5 버전 이력 (Git 방식 비교)
확정·수정할 때마다 버전이 **커밋처럼 쌓인다.**
- 버전 목록: 각 버전에 **확정자·시각·변경 사유·변경 유형(MAJOR/MINOR/PATCH)** 기록.
- **버전 비교(diff)**: 임의의 두 버전(base ↔ compare)을 골라 본문 변화를 **split diff(±)**로 확인(GitHub 커밋 diff 방식).
- 감사 추적: "언제·누가·어떤 근거로 이렇게 정리했는지"를 그대로 조회. 덮어쓰지 않고 누적.

### 3.6 버전 관리 모델 (Semantic Versioning)
확정본에 `MAJOR.MINOR.PATCH` 버전을 부여한다.

| 구분 | 예 | 의미 |
|---|---|---|
| **PATCH** | 1.0.0 → 1.0.1 | 문구·오타 다듬기 (의미 불변) |
| **MINOR** | 1.0.x → 1.1.0 | 해석 확정·조건 추가 (명확화) |
| **MAJOR** | 1.x → 2.0.0 | 요구 자체 변경·상충 해소 (의미 변경) |

### 3.7 검출 출력(계약) 예시
```json
{
  "req_id": "req-tf-01",
  "ambiguous": true,
  "findings": [
    { "span": "복수의 태스크 생성 허용 여부", "type": "정량 기준 부재", "reason": "허용 개수·조건이 정의되지 않아 해석이 갈림", "suggestion": "동일 자재 ID에 대해 최대 1건만 허용" }
  ],
  "conflicts": [
    { "with": "req-ta-01", "reason": "우선순위 규칙이 '요청 순' vs 'SoC 높은 순'으로 상반됨", "suggestion": "req-ta-01 기준으로 통일" }
  ]
}
```

---

## 4. 핵심 기능 2 — 산출물 도출

검토가 끝난 요구사항을, 우리 프로젝트에서 실제로 만들어야 하는 **문서·티켓(Jira 이슈)의 초안**으로 옮겨 준다.

### 4.1 도출 구조 — 요구사항 → 개발 이슈 N개 → 각 4종

```
확정 요구사항 1개
   └─(자동 도출, 1:N)─▶  개발 이슈(티켓) N개   ← 요구사항에 따라 1개 ~ N개
                              └─(1:1)─▶  개발 이슈 1개당 산출물 4종
                                          ├ SWVOC
                                          ├ 기능 요구사항
                                          ├ 비기능 요구사항
                                          └ Detail Design
```

- **요구사항 1개 → 개발 이슈 N개**: 하나의 요구사항이 여러 개발 단위(티켓)로 쪼개질 수 있다(1개일 수도, 여러 개일 수도).
- **개발 이슈 1개 → 4종 (각 1개씩)**: 개발 이슈(티켓)마다 아래 4종이 1:1로 붙는다.

**개발 이슈(티켓)** — 부모. 자체 본문은 3 카테고리: 〈요구사항 접수〉 현상기록·개선요청사항 / 〈요구사항 개발〉 변경범위·제약사항 / 〈변경점 설계〉 변경 전·후.

| 산출물(개발 이슈 1개당 1개) | 세부 항목 |
|---|---|
| **SWVOC** | 요청자 / 요청사항 / 특이사항 |
| **기능 요구사항** | 개요(역할·목적) / 동작 정의(9행) / 제약사항 |
| **비기능 요구사항** | 기능 요구사항과 동일 구조 (성능·신뢰성·가용성 등 품질 속성 대상) |
| **Detail Design** | 설명 / Class Diagram / Sequence Diagram(as-is·to-be) |

**동작 정의 = 9행**: 동작 유형 3가지(기본·변형·예외)마다 **선행조건·후행조건·시나리오**를 작성 → 총 9행. 기능·비기능 요구사항의 핵심.

### 4.2 작성 주체 — AI 초안 vs 사람
모두 AI가 뽑는 것은 아니다. **판단 기준**은 하나다.

> **이 항목을 「지금 받은 고객 요구서 + 정해진 문서 양식」만으로 채울 수 있는가?**
> - **예 → AI 초안**: 고객 요구를 요약·정형화·명세로 "옮기는" 일 (입력이 다 주어져 AI가 잘함)
> - **아니오 → 사람**: 현재 시스템 상태(as-is)를 알아야 하거나 설계·예외 판단이 필요 (AI가 알 수 없는 정보)

| 산출물 | 🤖 AI 초안 | 👤 사람 |
|---|---|---|
| 개발 이슈 | 개선요청·제약사항·변경 후(to-be) | 현상기록·변경범위·변경 전(as-is) |
| Detail Design | 설명·Sequence(to-be) | Class Diagram·Sequence(as-is) |
| SWVOC | 요청사항 | 요청자·특이사항 |
| 기능 요구사항 | 개요·동작 정의·제약사항 (전 항목) | (사람 검토·확정) |
| 비기능 요구사항 | 전 항목 | (사람 검토·확정) |

### 4.3 템플릿 + 사전 프롬프트, "틀 + 초안" 원칙
- **템플릿 고정**: 각 산출물의 필드·구조를 템플릿으로 고정.
- **유형별 프롬프트**: 산출물별 프롬프트를 미리 정의해 AI에 적용(예: "Detail Design을 채워라 — 시퀀스 다이어그램 포함").
- **AI 결과는 항상 초안**: 담당자가 채워진 틀 위에서 편집·추가·삭제한다.
- 각 산출물에 ID를 부여하고 근거 `REQ-ID`와 링크 → 기능 3(추적성)의 기반.

---

## 5. 핵심 기능 3 — 산출물 관리 · 추적성

요구사항과 산출물이 어떻게 연결돼 있는지 관리하고, 요구사항이 바뀌면 무엇이 영향받는지 알려준다.

### 5.1 연결 관리 (요구사항 ↔ 산출물)
- 요구사항 1건에 연결된 티켓·SWVOC·Function Requirement·Detail Design을 한눈에.
- 반대로 산출물에서 근거 요구사항을 역으로 조회.
- 연결은 원문 복사가 아니라 `REQ-ID ↔ 산출물 ID` **링크**로 유지.

### 5.2 변경 영향 분석
- 요구사항이 수정되면(= 새 버전), 그 문장을 근거로 만든 산출물을 자동으로 찾아 **영향받음 / 영향 없음**으로 표시.
- 예: "req-ta-01이 바뀌었다 → 영향: AMVCS30-77 티켓·FR·Detail Design / 영향 없음: SWVOC 원문·다른 티켓" — 누락을 막는다.

### 5.3 진행상황
- 요구사항·산출물·티켓의 상태(작성중·검토·개발중·완료)를 추적하고, 요구사항별·모듈별 진행률을 보여 준다.
- VCS APP은 8개 모듈(pathsearch/operation/jobassign/parametermanagement/hostinterface/mapupdater/watchdog/nats)로 나뉘므로 모듈별 정리·관리한다.

---

## 6. 사용자 · 인증

작성자 이력(누가 확정·수정했는지)을 남기기 위해 로그인이 필요하다.
- **회원가입 / 로그인**: 사번 + 비밀번호. (사내 SSO는 사용하지 않음)
- 모든 확정·수정·넘어가기 행위에 **작성자·시각**을 기록.
- 세션/토큰 기반 인증. 권한은 초기엔 단일 역할(작성자)로 시작.

---

## 7. 화면 구성 (기능 1 확정)

| 화면 | 경로 | 설명 |
|---|---|---|
| 로그인 · 회원가입 | `/login` | 사번+비밀번호, 회원가입 |
| 대시보드 (메인) | `/` | 통계 · 활동 피드 · 내 작업 · 최근 버전 |
| 요구사항 목록 | `/requirements` | 분류별 그룹 리스트(상태·버전·담당자) |
| 요구사항 검토 | `/requirements/{id}/review` | AI 검토 · 수정 · 변경 사유 · 확정(→ 새 버전) |
| 버전 이력 | `/requirements/{id}/versions` | 버전 목록 + base↔compare diff |
| 산출물 · 추적성 | (탭) | 기능 2·3 — 자리만 잡아 둠 |

좌측 네비게이션은 **앱 섹션**(대시보드·요구사항·산출물·추적성·활동·설정)이며, 3대 기능은 선택한 요구사항의 **탭**으로 동작한다. 상세는 [화면 정의서](docs/화면정의_기능1.html) 참고.

---

## 8. 로드맵 (8~11월)

과제 공식일정 사이사이에 개발을 2단계씩, 총 4단계로 진행.

| 마일스톤 | 시기 | 내용 |
|---|---|---|
| **과제계획** | 8/10 | 요구사항 검토 마무리 · AI 엔진 실측 · 양식 확정 |
| ① 산출물 도출 | 8월 중~말 | 확정 요구사항 → 산출물 5종 자동 도출 |
| ② 산출물 관리 | 9월 | 연결 관리 + 변경 추적 |
| **과제 중간보고** | 9/29 | 기능 2·3 데모 |
| ③ 외부 연동·통합 | 10월 | docx 파싱 입력 · Jira 연동 · 3기능 통합 |
| ④ 검증·안정화 | 11월 초중 | 파일럿 · 피드백 · 성능 튜닝 |
| **과제 최종보고** | 11월 말 | 결과·성과 정리 |

기능 1(요구사항 검토)은 8/10 계획 시점에 완료 목표.

---

## 9. 기대 효과 (정량 · 목표치)

| 항목 | 수작업 | 시스템 | 효과 |
|---|---|---|---|
| **하나의 기능에 대한 산출물 작성** | 평균 3시간 | 5분 이내 | 대폭 단축 |
| 요구사항 변경 시 영향 문서 찾기 | 수동 확인(누락 위험) | 영향 범위 자동 표시 | 누락 방지 |
| 불명확·상충 발견 시점 | 개발 중/후 | 착수 전 검토 | 재작업 감소 |

> 실측으로 확정할 목표/예시 수치.

---

## 10. 오픈 이슈

- [ ] 산출물 5종의 정확한 양식·필드 확정 → 템플릿·프롬프트화
- [ ] Detail Design 시퀀스 다이어그램 표현 방식(표기법·범위) 확정
- [ ] docx 표 → 요구사항(행) 단위 파싱 안정성 (공통 입력 리스크)
- [ ] Jira 연동 방식(반출 제약 하에서 이슈 생성/링크 자동화 범위)
