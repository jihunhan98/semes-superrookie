# Q&A

코드 관련 질문과 답변을 정리한 파일. 새 질문이 오면 이전 내용을 지우고 덮어쓴다.

---

## 지난 라운드 항목 중 문서/그림으로 처리한 것

- **"2 — 이슈 확정 후 UI가 별로다, 그림으로"**: 카드 그리드 시안을 Artifact로 만들었다 — https://claude.ai/code/artifact/5df72d9f-9867-4ad5-86df-16f819590dd2 ("이슈 카드뷰 제안"). 지금 코드에는 아직 반영 안 함(시안 단계).
- **"2.1 — 현상기록 등 AI가 다 하게, DESIGN.md에 반영"**: `DESIGN.md` §4.2 "작성 주체"를 고쳤다. 예전엔 "AI가 채울 수 있는가"로 항목을 AI/사람 둘로 나눴는데(현상기록·변경범위·변경 전은 사람 전담), 이제는 **모든 항목을 AI가 초안으로 채우고 사람은 항상 검토·수정만 한다**로 바꿨다. AI가 실제로 알 수 없는 항목(현재 시스템 as-is 등)은 빈칸이 아니라 "추정 — 확인 필요" 표시가 붙은 추정 초안으로 채운다.

아래는 이번에 새로 물어보신 두 가지에 대한 답변.

---

## 1.4 (이어서) — 처음 찾은 위치(오프셋)를 저장해두면, 나누기·합치기할 때 문제 안 생기나?

**결론: 생긴다.** 정확히는 "오프셋을 저장해두는 것" 자체는 문제없지만, 그 오프셋이 **의미를 잃는 시점**이 나누기·합치기 도중에 바로 온다.

1. **합치기(merge)** — 지금 로직(`mergeSelected`)은 체크된 카드들의 quote 텍스트를 그냥 이어 붙인다(`toMerge.map(quote).join(' ')`). 문제는 **연속되지 않은 카드끼리도 합칠 수 있다는 것** — 예를 들어 1번·3번 카드를 합치고 2번은 그대로 두면, 합쳐진 결과는 원문에서 **하나의 연속된 구간이 아니다**. 오프셋은 `[start, end]` 하나로 "원문의 한 구간"을 가리키는 모델이라, 이렇게 떨어진 두 구간을 합친 결과에는 애초에 오프셋 하나로 표현할 방법이 없다(구간 목록으로 바꿔야 하는데, 그러면 하이라이트 로직 전체가 복잡해진다).

2. **나누기(split)** — 지금은 사람이 카드의 quote 텍스트 자체를 자유롭게 고칠 수 있다(`<textarea className="quotein">`). 처음 AI가 준 그대로면 오프셋이 맞지만, 사람이 한 글자라도 고치는 순간 그 텍스트는 더 이상 원문의 `[start,end]` 구간과 일치하지 않는다 — 그러면 오프셋을 유지할지, 버릴지 정책이 필요해지고, 결국 "달라졌으니 다시 찾아야 한다"는 지금과 같은 문제로 되돌아간다.

3. 그래서 오프셋 저장이 실제로 도움되는 구간은 **"AI가 방금 제안했고 사람이 아직 아무것도 안 고친" 상태뿐**이다 — 그 상태에서만 정확하고, 편집·나누기·합치기가 한 번이라도 일어나면 다시 텍스트 재검색(지금 방식)이나 "위치 불명" 표시로 돌아가야 한다.

**그래서 지금 판단**: 오프셋 캐싱은 이득보다 복잡도(구간 목록 관리, 합치기 시 비연속 구간 처리, 무효화 시점 판단)가 더 크다고 보고 보류. 대신 나눠 볼 만한 개선은:
- 같은 문구가 여러 번 나올 때 "몇 번째 등장인지"만 오프셋으로 힌트를 주고, 텍스트 자체가 소스 오브 트루스인 건 유지 — 편집되면 그 힌트도 같이 버림.
- 위치를 못 찾을 때 지금처럼 조용히 "원문에서 위치를 찾지 못함"만 띄우는 대신, 가장 비슷한 구간을 찾아 "여기 근처였던 것 같음(추정)"으로 흐리게 하이라이트하는 정도.

## 2.3 (이어서) — 산출물(SWVOC 등)을 AI로 도출한다면 프롬프트를 어떻게 짤 것인가

아직 코드에는 없음(2.3 이전 답변 참고 — 지금은 100% 고정 mock). DESIGN.md §4.1~4.3과 `docs/screens/f3.html`의 파이프라인(1차: 이슈 분할 → 2차: 이슈 본문 → 3차: 산출물 4종, 이슈당 JSON 하나)을 그대로 따른다고 하면, 실제 `_SPLIT_SYSTEM_PROMPT`와 같은 형식으로 이렇게 짤 것이다(예시, 미구현):

```
당신은 반도체 장비 소프트웨어(VCS/AMR) 개발팀을 위해, 확정된 요구사항과 그 요구사항에서
나온 개발 이슈 하나를 근거로 산출물 4종(SWVOC·기능 요구사항·비기능 요구사항·Detail Design)의
초안을 작성하는 전문가다.

입력: 확정 요구사항 원문 / 이 이슈의 제목·커버 구절(quote) / 이 이슈 본문
(현상기록·개선요청사항·변경범위·제약사항·변경 전·후)

규칙:
- 모든 필드를 채운다. 빈 칸을 남기지 않는다.
- 현재 시스템의 실제 구현(Class Diagram, Sequence as-is 등)처럼 이 입력만으로는 알 수 없는
  항목은, 비슷한 이슈들의 패턴에 따라 합리적으로 추정하고 그 필드에 "assumed": true 를 붙인다.
- 기능/비기능 요구사항의 동작 정의는 반드시 기본·변형·예외 3유형 × 선행조건·시나리오·후행조건
  3항목 = 9행으로 채운다.
- 사실이 아닌 것을 지어내 근거인 것처럼 말하지 않는다 — 추정은 반드시 assumed로 표시한다.

반드시 아래 JSON 형식으로만 답한다:
{"voc": {"requester": "...", "request": "...", "notes": "..."},
 "functional": {"role": "...", "purpose": "...", "behaviors": [ {type, item, content} × 9 ], "constraints": "..."},
 "nonFunctional": {같은 구조 + constraints},
 "detailDesign": {"description": "...", "classDiagram": [...], "sequenceBefore": [...], "sequenceAfter": [...]}}
```

즉:
- 이슈 1개당 **1번 호출**로 4종을 한꺼번에 받는다(f3.html 설계와 동일 — 호출을 4번으로 쪼개지 않음).
- `analyze`/`split`과 같은 패턴으로 **환각 방지**: quote·클래스명 등은 입력에 등장한 것 위주로 검증하고, 안 나오는 새 정보(특히 as-is 관련)는 버리는 대신 "assumed" 플래그를 붙여 살려둔다(요구사항 검토처럼 조용히 버리면 안 됨 — as-is는 원래 추정해야 하는 영역이므로).
- 사내 LLM이 없으면 규칙 기반 폴백은 사실상 어렵다(요구사항 검토·이슈 분할과 달리, 산출물 4종은 패턴 매칭으로 만들 수 있는 게 거의 없음) — 이 부분은 `engine: unavailable`이면 "AI 미응답, 틀만 비어있는 채로 사람이 채워야 함"으로 남을 가능성이 높다는 점은 미리 고려해야 함.
