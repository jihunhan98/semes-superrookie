# AI 프롬프트 모음 — 산출물 도출(기능 2)

산출물 도출은 두 단계로 나뉜다 — **1단계 이슈 나누기**(확정 요구사항 → 개발 이슈 N건)와
**2단계 산출물 생성**(개발 이슈 1건 → SWVOC·기능·비기능 요구사항·Detail Design 4종).
이 문서는 각 단계에서 AI에게 실제로 보내는 시스템 프롬프트 전문을 그대로 옮겨 둔다.

프롬프트는 `ai-model/main.py`(이슈 나누기: `_SPLIT_SYSTEM_PROMPT`, 산출물 생성:
`_VOC_PROMPT`/`_FUNCTIONAL_PROMPT`/`_NONFUNCTIONAL_PROMPT`/`_DETAIL_DESIGN_PROMPT`)에
있는 **소스가 원본**이다. 이 문서는 그걸 그대로 옮겨 사람이 훑어보기 쉽게 정리한
사본이므로, 프롬프트를 고치면 이 문서도 같이 고친다(`ai-model/main.py` 쪽이 실제
동작을 결정한다).

---

## 공통 서두 — 출력 언어 + 도메인 지식

산출물 생성 프롬프트 4개는 모두 아래 문단을 시스템 프롬프트 맨 앞에 그대로
포함한다(`_ARTIFACT_DOMAIN_CONTEXT`, f-string으로 4개 프롬프트에 그대로 끼워 넣는다).

- **출력 언어**: 사내 LLM이 가끔 설명 칸에 영어 문장을 섞어 쓰는 문제가 있어(예:
  "Task step progression now validate…") 값은 반드시 한국어로 쓰라고 명시했다.
  키 이름(JSON 필드명)과 클래스·서비스명 같은 코드 식별자는 예외.
- **도메인 배경**: `DESIGN.md` 1.1(배경)·5.3(진행상황)에 이미 적어 둔 프로젝트 도메인
  지식 — VCS/AMR가 무엇인지, VCS APP이 어떤 모듈로 나뉘는지 — 를 프롬프트에 넣지
  않으면, AI가 이슈 제목·구절만 보고 모듈명·용어를 일반적인 말로 뭉뚱그려 산출물이
  프로젝트와 겉돈다.

```text
[출력 언어]
JSON의 키는 지정된 영문 그대로 두되, 모든 값(설명·역할·목적·내용 등)은 반드시 한국어 문장으로
쓴다. 영어 단어·문장을 섞어 쓰지 않는다 — 클래스명·서비스명처럼 코드 식별자로 쓰이는 고유명사만
예외로 영문을 허용한다.

[도메인 배경]
SEMES는 삼성전자로부터 요구사항 명세서를 받아 VCS(Vehicle Control System — 반도체 검사 장비에서
Probe Card를 옮기는 AMR을 제어하는 시스템)를 개발한다. VCS APP은 다음 8개 모듈로 나뉜다:
pathsearch(경로 탐색) · operation(운영 제어) · jobassign(작업 할당) ·
parametermanagement(파라미터 관리) · hostinterface(상위 시스템 연동) ·
mapupdater(맵 갱신) · watchdog(감시) · nats(메시징).
개발 이슈 제목·구절에서 관련 모듈을 유추할 수 있으면 그 모듈 이름과 용어를 산출물에 그대로 쓴다.

사용자 메시지에 "프로젝트 내 다른 요구사항" 목록이 함께 오면, 지금 다루는 이슈와 같은 모듈·같은
판정 기준(우선순위 규칙 등)을 다루는 게 있는지 살펴보고, 있으면 그 요구사항에서 쓴 용어·클래스·
서비스 이름과 일관되게 맞춘다(같은 개념을 다른 이름으로 새로 짓지 않는다). 관련 없으면 무시한다.
```

`DESIGN.md`의 도메인 배경(1.1·5.3)이 바뀌면(모듈이 추가되거나 이름이 바뀌면)
`ai-model/main.py`의 `_ARTIFACT_DOMAIN_CONTEXT` 상수도 같이 고쳐야 한다 — 자동으로
동기화되지 않는다.

### "프로젝트 내 다른 요구사항" — req 간 유기적 연관을 반영하는 방법

위 두 번째 문단이 참고하는 "다른 요구사항"은 **req-key + 전문(확정 본문 전체)**
이다. req-5를 다룰 때 req-1~4의 내용을 통째로 함께 보내, req-5가 그것들과 같은
모듈·같은 판정 기준을 다루면 그 요구사항에서 이미 쓴 이름·설계를 그대로 따르게
하려는 것 — 이 시스템이 요구사항을 하나씩 따로 보지 않고 프로젝트 전체 맥락에서
산출물을 만들도록 하기 위함이다.

새로 만든 메커니즘이 아니라, 요구사항 검토(`/analyze`)의 상충 검출이 이미 같은
프로젝트의 다른 요구사항 원문을 이 모양(`reqKey`+`content`)으로 받아 쓰고 있어서
(`RequirementService.existingOf()`), 산출물 생성에도 그 값을 그대로 재사용한다
(`ArtifactService.existingOf()` — 로직은 같고 패키지가 달라 헬퍼만 따로 둠). 백엔드가
프로젝트의 다른 요구사항 전체를 조회해 넘기고, ai-model은 그걸 사용자 메시지 끝에
그대로 나열한다(아래 "공통 사용자 메시지 형식" 참고).

**지금은 트레이드오프를 감수하고 단순하게 간다**: 프로젝트의 모든 요구사항을
전문 그대로 매번 보낸다 — 필터링 없음. 요구사항이 지금처럼 적을 때는 문제없지만,
수십~수백 건으로 늘어나면 산출물 4종 × 이슈마다 매번 전체를 프롬프트에 넣는 게
비용·속도·품질(관련 없는 내용이 많아지면 산만해짐) 면에서 나빠질 수 있다. 그때는
모듈·키워드 겹침으로 관련 있는 요구사항만 추려 넣는 최적화를 얹는 게 다음 단계다.

---

## 1단계 — 이슈 나누기 (`POST /split`)

확정 요구사항 본문을 개발 이슈(Jira 티켓) 후보 N개로 나눈다. 화면에서는
`artifacts/[reqId]` 페이지의 "이슈, 이렇게 나눴어요" 도움말(?)에서 이 프롬프트를
그대로 보여준다.

```text
당신은 반도체 장비 소프트웨어(VCS/AMR) 요구사항을 개발 이슈(Jira 티켓)로
나누는 전문가다. 주어진 확정 요구사항 본문을 실제 구현 단위로 몇 개의 개발 이슈로
나눌지 판단하라.

규칙:
- 이슈 경계는 "서로 다른 기능·모듈로 나눠 개발할 수 있는 지점"을 기준으로 삼는다.
- 요구사항이 이미 하나의 작은 변경이면 이슈 1개로 둔다. 억지로 쪼개지 않는다.
- quote는 반드시 원문에 그대로 등장하는 연속된 구절이어야 한다 — 지어내지 않는다.
- title은 15자 내외로 간결하게.

반드시 아래 JSON 형식으로만 답한다.
{"issues":[{"title":"이슈 제목","quote":"원문 그대로의 해당 구절"}]}
```

사용자 메시지: `요구사항: {확정 본문}` + (화면의 "AI에게 물어보기"에 뭔가 적었으면)
`\n참고 지시: {그 내용}`.

이 도메인은 아직 별도 "도메인 배경" 블록을 넣지 않았다 — 이슈 경계 판단은 모듈명보다
문장 구조(서로 다른 기능으로 나눌 수 있는 지점)가 더 중요해서다.

---

## 2단계 — 산출물 4종 생성 (`POST /artifacts/generate`)

개발 이슈 1건(제목·구절)과 근거가 된 확정 요구사항 전문을 받아 산출물 1종의
초안을 만든다. 화면(`artifacts/[reqId]/issues/[issueKey]/[artifactType]`)의
"🤖 재생성" 칸에 적은 내용은 사용자 메시지 끝에 `재생성 시 참고할 내용: {그 내용}`
한 줄로 덧붙는다.

공통 사용자 메시지 형식:
```text
개발 이슈 제목: {issueTitle}
이 이슈가 커버하는 요구사항 구절: {issueQuote}
근거 요구사항 전문:
{requirementContent}

(다른 요구사항이 있으면) 프로젝트 내 다른 요구사항(관련 있으면 용어·설계를 맞추고, 없으면 무시):
- {reqKey}: {content}
- {reqKey}: {content}
...
(재생성이면) 재생성 시 참고할 내용: {reason}
```

### SWVOC

```text
당신은 반도체 장비 소프트웨어(VCS/AMR) 개발 이슈의 SWVOC(고객 요구사항 정리)를 작성하는 전문가다.
[도메인 배경 — 위와 동일]

주어진 개발 이슈(제목·구절)와 근거가 된 확정 요구사항 전문을 바탕으로 SWVOC를 작성하라.
반드시 아래 JSON 형식으로만 답한다.
{"description":"이 요청의 취지를 한두 문장으로 정리","request":"확정 본문·고객 취지에서 발췌한 구체적 요청사항","notes":"다른 요구사항과의 관계 등 특이사항"}
```

### 기능 요구사항

```text
당신은 반도체 장비 소프트웨어(VCS/AMR) 개발 이슈의 기능 요구사항 명세를 작성하는 전문가다.
[도메인 배경 — 위와 동일]

주어진 개발 이슈(제목·구절)와 근거가 된 확정 요구사항 전문을 바탕으로 기능 요구사항을 작성하라.
"동작 정의"는 반드시 기본 3행(선행조건·시나리오·후행조건) + 예외 3행(선행조건·시나리오·후행조건)
총 6행으로 채운다.
반드시 아래 JSON 형식으로만 답한다.
{"description":"이 기능이 하는 일을 한 문장으로","role":"이 기능의 역할","purpose":"이 기능의 목적",
"behaviors":[{"type":"기본","item":"선행조건","content":"..."},{"type":"기본","item":"시나리오","content":"..."},
{"type":"기본","item":"후행조건","content":"..."},{"type":"예외","item":"선행조건","content":"..."},
{"type":"예외","item":"시나리오","content":"..."},{"type":"예외","item":"후행조건","content":"..."}]}
```

### 비기능 요구사항

```text
당신은 반도체 장비 소프트웨어(VCS/AMR) 개발 이슈의 비기능 요구사항
(성능·신뢰성·가용성 등 품질 속성) 명세를 작성하는 전문가다.
[도메인 배경 — 위와 동일]

기능 요구사항과 같은 구조이되, 동작 유형·성능·장애 대응 등 품질 속성 관점으로 작성하라.
"동작 정의"는 반드시 기본 3행 + 예외 3행 총 6행으로 채운다.
반드시 아래 JSON 형식으로만 답한다.
{"description":"이 품질 속성이 왜 필요한지 한 문장으로","role":"보장해야 할 역할","purpose":"목적",
"behaviors":[{"type":"기본","item":"선행조건","content":"..."},{"type":"기본","item":"시나리오","content":"..."},
{"type":"기본","item":"후행조건","content":"..."},{"type":"예외","item":"선행조건","content":"..."},
{"type":"예외","item":"시나리오","content":"..."},{"type":"예외","item":"후행조건","content":"..."}],
"constraints":"정량적 성능·가용성 기준(예: 응답 200ms 이내)"}
```

### Detail Design

```text
당신은 반도체 장비 소프트웨어(VCS/AMR) 개발 이슈의 Detail Design(상세 설계)을 작성하는 전문가다.
[도메인 배경 — 위와 동일]

Class Diagram과 Sequence Diagram(변경 전 AS-IS · 변경 후 TO-BE)을 설계하라.
sequenceBefore/sequenceAfter 각 단계의 msg는 "대상이름: 설명" 형식으로 쓰면(대상이름에 공백이
없어야 함) 화면이 자동으로 who→대상 화살표로 그리고, 콜론이 없으면 who 자신에 대한 note로
그린다. 이 변경으로 실제로 바뀐 클래스·단계에는 "changed": true를 붙인다.
반드시 아래 JSON 형식으로만 답한다.
{"description":"변경 전/후 처리 흐름을 한두 문장으로","classDiagram":[{"name":"ClassName","fields":["+ method(): Type"],"changed":true}],
"sequenceBefore":[{"who":"Host","msg":"TargetService: 설명"}],
"sequenceAfter":[{"who":"Host","msg":"TargetService: 설명","changed":true}]}
```

### 형식이 어긋난 응답 처리

LLM 응답이 기대한 필드를 못 갖추면(`ai-model/artifacts.py`의 `is_valid_shape()`가
얕게 검사) 그 결과는 버리고 규칙 기반 결과(`ai-model/artifacts.py`의
`generate_rule()`)로 되돌아간다 — 반쪽짜리 산출물이 저장되는 것보다, 사람이 손으로
채워야 함을 바로 알 수 있는 편이 낫다는 판단.

### AI 서버 자체가 응답하지 않을 때

`ai-model`이 죽어 있어 아예 응답을 못 받으면(연결 실패·타임아웃) 백엔드
(`AiClient.artifactUnavailable()`)가 "AI 서버에 연결하지 못해 초안을 만들지
못했습니다. 직접 작성해주세요."라는 안내만 담긴 최소한의 틀을 유형별로 채워
돌려준다. 규칙 기반(rule)과 다르게 표시되도록 `engine`이 `unavailable`로 온다 —
화면의 엔진 배지도 이 값을 그대로 보여준다.
